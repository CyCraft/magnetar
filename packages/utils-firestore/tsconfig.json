{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    // This must be specified if "paths" is set
    "baseUrl": "."
  },
  "include": ["src/**/*", "test/**/*", "../plugin-firestore/src/helpers/queryHelpers.ts"]
}
