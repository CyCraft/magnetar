/**
 * Tries to find a value of a map for a given key. The key does not have to be the original payload passed.
 */
export declare function findMapValueForKey(map: Map<any, any> | undefined, mapKey: any): any;
