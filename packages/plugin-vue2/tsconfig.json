{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "declarationDir": "./types/",
    // This must be specified if "paths" is set
    "baseUrl": ".",
    // Relative to "baseUrl"
    "paths": {
      "@magnetarjs/core": ["../../core/index.ts"]
    }
  },
  "include": ["src/**/*", "test/**/*"]
}
