{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "declarationDir": "./dist/types/",
    // This must be specified if "paths" is set
    "baseUrl": "."
  },
  "include": ["src/**/*", "test/**/*"]
}
